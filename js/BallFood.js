
class BallFood extends Ball {
  /**
   * 
   * @param {Stage} stageB
   * @param {objet{x,y}} speed
   * @param {sting} color
   * @param {int} radius
   * @returns {BallFood}
   */
  constructor(stageB, speed, color , radius) {
    super(stageB, speed, color , radius); 
  }
  
}